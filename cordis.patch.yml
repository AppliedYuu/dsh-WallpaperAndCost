# dsh-WallpaperAndCost bundle patch: mounts the host half as one loader row.
# The browser half is discovered automatically through the package's
# `dsh.client` declaration + `exports["./client"]`.
- insert:
    - id: dsh-WallpaperAndCost
      name: dsh-WallpaperAndCost
